name: PR Checks

# Este workflow está siendo reemplazado por ci.yml
# Se mantiene para compatibilidad, pero usa ci.yml para las verificaciones

on:
  pull_request:
    branches: [master, main, develop]

jobs:
  pr-checks:
    name: PR Quality Checks (Legacy)
    runs-on: ubuntu-latest
    # Este job ahora solo referencia el workflow principal
    # Las verificaciones reales están en .github/workflows/ci.yml

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Note
        run: |
          echo "⚠️ Este workflow está siendo reemplazado por ci.yml"
          echo "Las verificaciones principales se ejecutan en el workflow CI"
          echo "Este workflow se mantiene solo para compatibilidad"

